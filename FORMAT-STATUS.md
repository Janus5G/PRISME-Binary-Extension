# Format status

Version 0.1 is experimental. Header extensions, signature blocks, compression profiles, schema registries, chunked containers and transport APIs are not yet frozen. Implementations must reject unsupported major versions.
